<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ABSSOLUTIONS" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/amber_accent</item>
        <item name="colorPrimaryVariant">@color/amber_accent</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="android:colorBackground">@color/dark_background</item>
        <item name="colorSurface">@color/dark_element</item>
        <item name="colorOnSurface">@android:color/white</item>
        <item name="colorSecondary">@color/dark_element</item>
        <item name="colorOnSecondary">@android:color/white</item>
        <item name="colorError">@color/red_error</item>
        <item name="colorOnError">@android:color/white</item>

        <!-- Optional: Customize text appearances -->
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@color/darker_gray_text</item>
        <item name="android:textColorHint">@color/darker_gray_text</item>

    </style>

    <style name="Theme.ABSSOLUTIONS" parent="Base.Theme.ABSSOLUTIONS" />
</resources>